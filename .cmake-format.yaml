# Optional knobs for cmake-format (pre-commit hook).
# https://cmake-format.readthedocs.io/
format:
  line_width: 100
  tab_size: 2
  max_subgroups_hwrap: 2
  max_pargs_hwrap: 4
  dangle_parens: true
  command_case: canonical
  keyword_case: upper
